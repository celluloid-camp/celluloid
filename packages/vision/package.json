{
  "name": "@celluloid/vision",
  "version": "3.3.3",
  "private": true,
  "type": "module",
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "postinstall": "bun run --env-file ../../.env generate",
    "build": "bun run --env-file ../../.env generate",
    "generate": "bun run --env-file ../../.env orval --config ./orval.config.ts || true",
    "clean": "git clean -xdf .cache .turbo dist node_modules"
  },
  "devDependencies": {
    "@celluloid/typescript-config": "workspace:*",
    "typescript": "catalog:"
  },
  "dependencies": {
    "orval": "^7.10.0",
    "@t3-oss/env-core": "catalog:",
    "zod": "catalog:"
  }
}
