/** Public PeerTube video used by create-project e2e (must resolve via /api/v1/videos/:id). */
export const E2E_PEERTUBE_VIDEO_URL =
  "https://celluloid.cloud/w/5m3BEnByo1dsPMKvRGQtRH";

/** Forced by generateOTP when E2E / CI_TEST is set on the server. */
export const E2E_OTP = "123456";
