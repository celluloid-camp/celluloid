name: E2E Tests

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  pull_request:
    branches: [staging]
    paths:
      - "apps/web/**"
      - "packages/**"
      - "e2e/**"
      - "compose.ci.yml"
      - "playwright.config.ts"
      - ".github/workflows/test-ci.yml"
  workflow_dispatch:

jobs:
  tests:
    if: >
      github.event_name == 'workflow_dispatch' ||
      (github.actor != 'renovate[bot]' && !startsWith(github.head_ref, 'renovate/'))
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Cache Bun
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - uses: oven-sh/setup-bun@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build web container
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./apps/web/Dockerfile
          load: true
          tags: celluloid-web:latest
          cache-from: type=gha,scope=celluloid-web-ci
          cache-to: type=gha,mode=max,scope=celluloid-web-ci
          build-args: |
            VERSION=ci
            COMMIT_SHA=${{ github.sha }}
            BUILD_TIME=${{ github.run_started_at }}
            NEXT_PUBLIC_STAGE=testing

      - name: Build db migrate container
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./packages/db/Dockerfile
          load: true
          tags: celluloid-db-migrate:latest
          cache-from: type=gha,scope=celluloid-db-migrate-ci
          cache-to: type=gha,mode=max,scope=celluloid-db-migrate-ci
          build-args: |
            VERSION=ci
            COMMIT_SHA=${{ github.sha }}
            BUILD_TIME=${{ github.run_started_at }}

      - name: Install Playwright Browsers
        run: bunx playwright install --with-deps chromium firefox

      - name: Install Playwright test dependency
        run: bun install @playwright/test

      - name: Start services with Docker Compose (wait for healthchecks)
        run: docker compose -f compose.ci.yml up -d --wait --no-build

      - name: Show container logs on failure
        if: failure()
        run: docker compose -f compose.ci.yml logs --no-color

      - name: Smoke-check web responds
        run: |
          curl -sfS http://127.0.0.1:3000/api/health
          curl -sfS -o /dev/null -w "%{http_code}\n" http://127.0.0.1:3000/signup | tee /tmp/signup-status.txt
          test "$(cat /tmp/signup-status.txt)" = "200"

      - name: Run Playwright tests
        env:
          CI: true
          CI_TEST: true
          E2E: "1"
          PLAYWRIGHT_BASE_URL: http://127.0.0.1:3000
        run: bun --env-file=apps/web/.env.ci playwright test

      - name: Dump web logs after tests
        if: failure()
        run: docker compose -f compose.ci.yml logs web --no-color | tail -n 200

      - name: Stop services
        if: always()
        run: |
          docker compose -f compose.ci.yml down -v

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: test-results/
          retention-days: 5

      - name: Comment PR with test results
        uses: dorny/test-reporter@v1
        if: always() && github.event_name == 'pull_request'
        with:
          name: Playwright E2E Tests
          path: test-results/junit.xml
          reporter: java-junit
          list-suites: all
          list-tests: all
          max-annotations: 50
          fail-on-error: true
          only-summary: false
